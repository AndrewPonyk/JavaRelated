[project]
name = "dynamic-programming-algorithms"
version = "0.1.0"
description = "Dynamic programming algorithms in Python and Java for learning."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["python"]
testpaths = ["python/tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
