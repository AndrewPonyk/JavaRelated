name: .NET CI

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
    branches: [ "main", "develop" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x
        
    - name: Restore dependencies
      run: dotnet restore src/backend/EnterpriseInsurance.Api/EnterpriseInsurance.Api.csproj
      
    - name: Build
      run: dotnet build src/backend/EnterpriseInsurance.Api/EnterpriseInsurance.Api.csproj -c Release --no-restore
      
    - name: Test
      run: dotnet test src/backend/EnterpriseInsurance.Tests/EnterpriseInsurance.Tests.csproj -c Release --no-build --verbosity normal
