aiokafka~=0.11
redis~=5.0
pydantic~=2.8
pydantic-settings~=2.4

# TODO(Phase 2):
# river~=0.21            # online anomaly models (HalfSpaceTrees) for the retrain loop
# boto3~=1.34            # SNS direct publish + SSM model-version polling
# snowflake-connector-python  # NOT needed in prod — warehouse ingest goes via MSK Connect
