-r requirements.txt

# ── Testing ──────────────────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.25.3
pytest-cov==6.0.0
aiosqlite==0.20.0                 # in-memory async DB for the unit/API suite
moto[s3]==5.0.18                  # mock S3 so storage tests need no MinIO
testcontainers[postgres]==4.5.1   # real Postgres + MinIO in integration tests
schemathesis==3.33.0              # OpenAPI / DICOMweb contract fuzzing

# ── Lint / format / types (versions verified to pass the gate) ─
ruff==0.8.4
mypy==1.8.0
black==23.12.1
detect-secrets==1.5.0
pip-audit==2.7.3                   # dependency vulnerability audit (used in CI)

# ── Type stubs ───────────────────────────────────────────────
boto3-stubs[s3,sqs]==1.34.131
types-python-jose==3.3.4.20240106
