# ── Web framework ────────────────────────────────────────────
# fastapi >= 0.115 required for Pydantic-model query params (Annotated[Model, Query()]).
fastapi==0.115.6
uvicorn[standard]==0.30.1
python-multipart==0.0.9          # DICOMweb multipart / file uploads

# ── Config & validation ──────────────────────────────────────
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0           # required by pydantic EmailStr (login schema)

# ── Persistence ──────────────────────────────────────────────
SQLAlchemy[asyncio]==2.0.36
asyncpg==0.29.0                  # async PostgreSQL driver
alembic==1.13.2

# ── DICOM ────────────────────────────────────────────────────
pydicom==2.4.4
numpy==1.26.4
pylibjpeg==2.0.0                 # compressed transfer-syntax plug-in...
pylibjpeg-libjpeg==2.1.0         # ...JPEG
pylibjpeg-openjpeg==2.2.0        # ...JPEG2000

# ── AWS / object store / queue ───────────────────────────────
boto3==1.34.131                  # S3 (MinIO-compatible) + SQS

# ── Security ─────────────────────────────────────────────────
python-jose[cryptography]==3.3.0 # JWT
passlib[bcrypt]==1.7.4           # password hashing
bcrypt==4.1.3                    # explicit pin (passlib bcrypt backend)

# ── Observability ────────────────────────────────────────────
structlog==24.2.0
httpx==0.27.0                    # ML service client + test client
