// @cornerstonejs/dicom-image-loader@1.x ships a UMD bundle with no type
// declarations. Declare it as an untyped module so the (classic) external /
// configure / wadouri API is usable under strict TypeScript.
declare module '@cornerstonejs/dicom-image-loader';
