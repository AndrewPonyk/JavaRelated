/**
 * Application JavaScript.
 *
 * Livewire 3 ships and boots Alpine.js itself via the @livewireScripts
 * directive in the layout, so we do NOT import or start Alpine here (doing both
 * would boot Alpine twice). This file is the place for any custom, app-specific
 * browser code that should load on every page.
 *
 * Dark-mode persistence is handled inline in the layout with localStorage, so
 * no Alpine plugins are required.
 */
