<?php

declare(strict_types=1);

namespace App\Http\Controllers;

/**
 * Base controller. In Laravel 11 the framework no longer ships an abstract base
 * with traits by default — add shared traits (AuthorizesRequests, etc.) here.
 */
abstract class Controller
{
    //
}
