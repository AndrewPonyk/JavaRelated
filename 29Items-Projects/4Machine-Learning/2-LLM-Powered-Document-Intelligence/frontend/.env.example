# Frontend environment template. Only NEXT_PUBLIC_* values reach the browser —
# never put API keys or secrets here.
NEXT_PUBLIC_API_BASE_URL=/api/v1
# Server-side rewrite target (see next.config.mjs); not exposed to the browser.
BACKEND_URL=http://localhost:8000
