# ── Core web framework ──────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4

# ── Persistence (pgvector reference backend) ────────────
sqlalchemy[asyncio]==2.0.31
asyncpg==0.29.0
psycopg[binary]==3.2.1        # sync driver for Alembic migrations
alembic==1.13.2
pgvector==0.3.0
aiosqlite==0.20.0             # async sqlite driver (local dev + tests)

# ── Cache ───────────────────────────────────────────────
redis==5.0.7

# ── Embeddings ──────────────────────────────────────────
sentence-transformers==3.0.1

# ── Logging ─────────────────────────────────────────────
structlog==24.2.0

# ── Vector backends (feature-flagged; pin versions) ─────
pinecone-client==4.1.1
weaviate-client==4.6.5
pymilvus==2.4.4

# ── Dev / test (see requirements-dev.txt if you split) ──
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0
httpx==0.27.0
ruff==0.5.1
mypy==1.10.1
