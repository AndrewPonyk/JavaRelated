# Dev/test dependencies — installed on top of requirements.txt
-r requirements.txt
pytest>=8.2,<9
pytest-asyncio>=0.23,<1
pytest-cov>=5.0,<7
respx>=0.21,<1                  # httpx mocking for scanner clients & webhook
aiosqlite>=0.20,<1              # async SQLite test DB
pip-audit>=2.7,<3               # dependency CVE gate (CI runs it with --no-deps)
