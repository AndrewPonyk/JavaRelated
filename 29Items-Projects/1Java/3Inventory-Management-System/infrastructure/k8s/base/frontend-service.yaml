apiVersion: v1
kind: Service
metadata:
  name: inventory-frontend
  namespace: inventory
spec:
  selector:
    app.kubernetes.io/name: inventory-frontend
  ports:
    - { name: http, port: 8080, targetPort: http }

