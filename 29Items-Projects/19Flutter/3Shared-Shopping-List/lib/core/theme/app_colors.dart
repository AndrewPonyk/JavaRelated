import 'package:flutter/material.dart';

class AppColors {
  // Brand Primary: Fresh Emerald Green
  static const Color primary = Color(0xFF0F9D58);
  static const Color primaryContainer = Color(0xFFE8F5E9);

  // Secondary: Warm Amber / Tangerine
  static const Color secondary = Color(0xFFFB8C00);
  static const Color secondaryContainer = Color(0xFFFFF3E0);

  // Surface & Neutrals
  static const Color backgroundLight = Color(0xFFF8F9FA);
  static const Color surfaceLight = Colors.white;
  static const Color textPrimary = Color(0xFF1E293B);
  static const Color textSecondary = Color(0xFF64748B);

  // Status & Sync Indicators
  static const Color gotItSuccess = Color(0xFF16A34A);
  static const Color syncPending = Color(0xFFEAB308); // Amber sync badge
  static const Color error = Color(0xFFDC2626);
}
