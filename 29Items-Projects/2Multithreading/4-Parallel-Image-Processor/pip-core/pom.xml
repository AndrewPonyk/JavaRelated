<?xml version="1.0" encoding="UTF-8"?>
<!--
  pip-core — the domain.

  INVARIANT: the <dependencies> block below contains ONLY test-scoped entries.
  Adding a compile-scope dependency here breaks the hexagonal boundary documented
  in docs/ARCHITECTURE.md §2.1. If you need SQLite, JavaFX, or OpenCV, declare a
  PORT (interface) here and implement it in pip-persistence / pip-ui / pip-native.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.parallelimage</groupId>
    <artifactId>pip-parent</artifactId>
    <version>1.0.0-SNAPSHOT</version>
  </parent>

  <artifactId>pip-core</artifactId>
  <name>PIP :: Core (domain + fork/join engine)</name>

  <!-- No compile-scope dependencies. JDK only. -->

  <build>
    <plugins>
      <!-- pip-core carries the strictest coverage gate: it holds all the logic
           that is hard to debug once it is wrong. -->
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>coverage-gate</id>
            <phase>verify</phase>
            <goals><goal>check</goal></goals>
            <configuration>
              <haltOnFailure>false</haltOnFailure>
              <rules>
                <rule>
                  <element>BUNDLE</element>
                  <limits>
                    <limit>
                      <counter>LINE</counter>
                      <value>COVEREDRATIO</value>
                      <minimum>${coverage.line.min}</minimum>
                    </limit>
                    <limit>
                      <counter>BRANCH</counter>
                      <value>COVEREDRATIO</value>
                      <minimum>${coverage.branch.min}</minimum>
                    </limit>
                  </limits>
                </rule>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>mutation</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.pitest</groupId>
            <artifactId>pitest-maven</artifactId>
            <version>1.15.8</version>
            <dependencies>
              <dependency>
                <groupId>org.pitest</groupId>
                <artifactId>pitest-junit5-plugin</artifactId>
                <version>1.2.1</version>
              </dependency>
            </dependencies>
            <configuration>
              <targetClasses>
                <param>com.parallelimage.core.*</param>
              </targetClasses>
              <targetTests>
                <param>com.parallelimage.core.*</param>
              </targetTests>
              <mutationThreshold>70</mutationThreshold>
              <timestampedReports>false</timestampedReports>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
