:concurrency: <%= ENV.fetch("SIDEKIQ_CONCURRENCY", 10).to_i %>
:timeout: 25
:queues:
  - [critical, 3]
  - [default, 2]
  - [mailers, 2]
  - [low, 1]

:scheduler:
  :enabled: true

:schedule:
  workload_predictions_nightly:
    cron: "0 2 * * *"
    class: WorkloadPredictionBatchJob
    queue: low
