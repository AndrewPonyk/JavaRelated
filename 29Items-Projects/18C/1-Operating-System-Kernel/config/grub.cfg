# grub.cfg — GRUB menu entry for the bootable ISO (make iso).
#
# GRUB loads our Multiboot2 kernel directly. `boot` immediately after to avoid
# the interactive menu timeout in CI.

set timeout=0
set default=0

menuentry "microkernel" {
    multiboot2 /boot/kernel.elf
    boot
}
