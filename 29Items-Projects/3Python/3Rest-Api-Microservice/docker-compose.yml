# |su:70) DOCKER COMPOSE - Orchestrate multiple containers (api, db, phpmyadmin) with one command
version: '3.8'

services:
  #  |su:71) API SERVICE - Flask app container, depends on db being healthy first
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: customer-api
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
      - DATABASE_URL=mysql://root:rootpassword@db:3306/customer_api
      - SECRET_KEY=dev-secret-key-change-in-production
      - HF_CACHE_DIR=/app/models
    volumes:
      - ./app:/app/app:ro
      - ./migrations:/app/migrations
      - model_cache:/app/models
    depends_on:
      db:
        condition: service_healthy
    networks:
      - app-network
    restart: unless-stopped

  # |su:72) DATABASE SERVICE - MySQL container with health check so API waits until ready
  db:
    image: mysql:8.0
    container_name: customer-db
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: customer_api
      MYSQL_CHARACTER_SET_SERVER: utf8mb4
      MYSQL_COLLATION_SERVER: utf8mb4_unicode_ci
    ports:
      - "3306:3306"
    # |su:73) VOLUMES - Persist data outside container. Named volume mysql_data survives restarts --c
    volumes:
      - mysql_data:/var/lib/mysql
      - ./scripts/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    # |su:74) HEALTHCHECK - Docker checks if service is ready. API uses 'condition: service_healthy'
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - app-network
    restart: unless-stopped

  # Optional: phpMyAdmin for database management
  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: customer-phpmyadmin
    environment:
      PMA_HOST: db
      PMA_PORT: 3306
      UPLOAD_LIMIT: 50M
    ports:
      - "9090:80"
    depends_on:
      - db
    networks:
      - app-network
    profiles:
      - tools

volumes:
  mysql_data:
  model_cache:

networks:
  app-network:
    driver: bridge
