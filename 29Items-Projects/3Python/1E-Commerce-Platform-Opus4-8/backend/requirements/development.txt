# Development + test tooling. Never installed into the production image,
# so these are outside the deployed attack surface.
-r base.txt

# Test
pytest==9.0.3
pytest-django==4.11.1
pytest-cov==5.0.0
pytest-mock==3.14.0
factory-boy==3.3.1

# Lint / format / type-check
ruff==0.4.4
black==26.3.1
mypy==1.14.1
django-stubs==5.1.3  # matches Django 5.2 line

# Security auditing
pip-audit==2.7.3

# Load testing
locust==2.27.0
