# Dev: single replica for the active (blue) deployment.
# Green stays at 0 - the CD pipeline scales it during blue-green rollouts.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fraud-api-blue
spec:
  replicas: 1
