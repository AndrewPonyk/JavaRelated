Here’s the **revised, ultra-detailed table** with **expanded descriptions and business logic** for each project, while maintaining all constraints (Kafka in 10+ projects, ML/DL in AWS, Oracle/SQL Server/PostgreSQL/MySQL counts, etc.).

---

| **Topic** | **Size** | **Project Name** | **Tech Stack** | **Short Tech Description** | **Business Logic** | **IDE** | **Deployment Platform and CI/CD Tools** |
|-----------|----------|------------------|----------------|-----------------------------|--------------------|---------|------------------------------------------|
| **Java** | B | Enterprise Resource Planning (ERP) System | Java 21, Spring Boot, Jakarta EE, Hibernate, Maven, JUnit 5, Micronaut, SLF4J, GraalVM (Native-Image), Testcontainers, Grafana, Swagger/OpenAPI, Flyway, WebFlux/Reactor, **Oracle DB**, **Kafka**, Redis | A **modular, microservices-based ERP system** built on **Spring Boot** for business logic, **Hibernate** for ORM, and **GraalVM (Native-Image)** for fast startup and low memory footprint. **WebFlux/Reactor** enables reactive programming to handle high concurrency, while **Oracle DB** ensures ACID-compliant transactions. **Kafka** streams real-time updates across modules (e.g., finance, HR, supply chain), and **Redis** caches frequently accessed data. **Flyway** manages database migrations, **Swagger/OpenAPI** documents APIs, and **Grafana** visualizes system metrics. | Centralizes company-wide operations, including **finance (ledger, payroll)**, **HR (employee records, payroll)**, **supply chain (inventory, logistics)**, and **CRM (customer interactions, sales pipelines)**. Integrates with **third-party APIs** (e.g., payment gateways like Stripe, logistics providers like FedEx) via **REST/gRPC**. **Kafka** ensures real-time synchronization of data across modules, such as updating inventory levels after a sale or triggering payroll calculations. **Testcontainers** spins up isolated DB instances for testing, ensuring reliability. | IntelliJ IDEA Ultimate | AWS EKS (Kubernetes), GitHub Actions, ArgoCD |
| **Java** | B | Real-Time Analytics Platform | Java 21, Quarkus, Vert.x, Hibernate, Gradle, JUnit 5, SLF4J, GraalVM (JIT+Native-Image), Testcontainers, Prometheus, Grafana, Swagger/OpenAPI, Liquibase, **PostgreSQL**, **Kafka** | A **low-latency, cloud-native analytics platform** leveraging **Quarkus** for rapid startup and **Vert.x** for event-driven, non-blocking I/O. **GraalVM (JIT+Native-Image)** optimizes runtime performance, while **Hibernate** maps data to **PostgreSQL**, which stores time-series and relational data. **Kafka** ingests high-velocity event streams (e.g., IoT sensor data, user clicks), and **Prometheus/Grafana** monitor system health. **Liquibase** handles schema migrations, and **Swagger/OpenAPI** documents endpoints. | Ingests and processes **real-time data streams** from **IoT devices** (e.g., temperature, pressure) and **user interactions** (e.g., clicks, purchases) to generate actionable insights. Uses **PyTorch (via ONNX)** for predictive analytics, such as forecasting equipment failures or user churn. **Kafka** buffers and routes events to consumers, while **Vert.x** ensures non-blocking processing. Business users access dashboards to track KPIs like **conversion rates, anomaly detection, and trend analysis**. | IntelliJ IDEA Ultimate | AWS EKS, Jenkins, Helm |
| **Java** | M | E-Commerce Backend with Microservices | Java 17, Spring Boot, Hibernate, Maven, JUnit 5, SLF4J, Testcontainers, Swagger/OpenAPI, **MySQL**, **Kafka**, Elasticsearch | A **microservices-based e-commerce backend** built with **Spring Boot** for modularity and **Hibernate** for ORM, connected to a **MySQL** database for product and user data. **Kafka** handles asynchronous order processing and inventory updates, while **Elasticsearch** powers fast, full-text product searches. **Swagger/OpenAPI** documents the RESTful APIs, and **Testcontainers** ensures isolated testing. | Manages **user authentication (JWT/OAuth2)**, **product catalogs (CRUD operations, categories, filters)**, **shopping carts (session management)**, **order processing (payment integration, status tracking)**, and **payment gateways (Stripe, PayPal)**. **Kafka** decouples services: for example, the **order service** publishes events (e.g., "order_created") to update **inventory** and trigger **email notifications**. **Elasticsearch** indexes product data for near-instant search results. | IntelliJ IDEA Ultimate | Docker Swarm, GitLab CI/CD |
| **Java** | M | Healthcare Appointment Scheduling System | Java 17, Jakarta EE, Hibernate, Gradle, JUnit 5, SLF4J, Testcontainers, Swagger/OpenAPI, **Oracle DB**, **Kafka** | A **Jakarta EE-based healthcare system** for managing **patient appointments, doctor schedules, and medical records**. **Oracle DB** ensures ACID compliance for critical data like patient histories, while **Kafka** enables real-time notifications (e.g., appointment reminders, lab result alerts). **Hibernate** simplifies database interactions, and **Swagger/OpenAPI** provides API documentation. | Allows **patients** to **book, reschedule, or cancel appointments** with doctors, view medical histories, and receive automated reminders. **Doctors** can manage their schedules, view patient records, and update diagnoses. **Admins** generate reports (e.g., no-show rates, doctor utilization). Integrates with **FHIR (Fast Healthcare Interoperability Resources) APIs** to exchange data with other healthcare systems (e.g., labs, pharmacies). **Kafka** streams events like **appointment confirmations** or **prescription updates** to relevant parties. | Eclipse IDE | Azure Kubernetes Service (AKS), Azure DevOps |
| **Multithreading Java** | B | High-Frequency Trading Platform | Java SE 21, Thread Pools, Atomic Operations, Locks, Semaphores, G1 GC, **Kafka**, **Oracle DB** | A **low-latency trading platform** designed for **sub-millisecond order processing**. Uses **Java SE concurrency primitives** like **ReentrantLock** for thread-safe order matching, **AtomicLong** for non-blocking counters, and **Semaphores** to limit access to shared resources. **G1 GC** is tuned for minimal pause times to avoid disrupting trading. **Kafka** ingests **market data feeds** (e.g., stock prices, order book updates), and **Oracle DB** stores transaction records with ACID guarantees. | Processes **thousands of orders per second** with **sub-millisecond latency**, matching buy/sell orders in real-time. Implements the **producer-consumer pattern** to decouple order submission from execution, and **deadlock-free algorithms** to prevent system hangs. **Race conditions** are avoided using **synchronized blocks** and **atomic variables**. **Starvation** is mitigated by prioritizing threads fairly. **Kafka** distributes market data to trading algorithms and risk management systems. | IntelliJ IDEA Ultimate | On-Premise (Linux), Jenkins, Ansible |
| **Multithreading Java** | B | Distributed Task Scheduler | Java SE 21, Thread Pools, Starvation-Free Algorithms, Producer-Consumer, ZGC, **DynamoDB**, **Kafka** | A **distributed task scheduler** that executes background jobs (e.g., report generation, data backups) across a cluster. Uses **Java SE concurrency utilities** like **ExecutorService** for thread pooling, **Phaser** for synchronization, and **starvation-free algorithms** to ensure fair resource allocation. **ZGC** manages large heaps efficiently, while **DynamoDB** stores task metadata and **Kafka** distributes tasks to worker nodes. | Schedules and executes **background jobs** (e.g., generating financial reports, backing up databases) across a **cluster of machines**. Handles **resource contention** by dynamically adjusting thread priorities and **thread starvation** by ensuring no thread is indefinitely blocked. **Producer-consumer queues** balance workloads, and **Kafka** ensures tasks are reliably delivered to workers. **DynamoDB** tracks task statuses (e.g., pending, completed, failed). | IntelliJ IDEA Ultimate | AWS EC2, GitHub Actions |
| **Multithreading Java** | M | Multi-Threaded Web Crawler | Java SE 17, Thread Pools, Semaphores, Atomic Operations, Shenandoah GC, **MongoDB** | A **scalable web crawler** that fetches and indexes web pages in parallel. Uses **ForkJoinPool** for task parallelism, **CountDownLatch** for synchronization, and **Semaphores** to limit concurrent connections to a domain. **Shenandoah GC** reduces pause times to avoid crawling interruptions. **MongoDB** stores crawled data (e.g., URLs, page content, metadata), and **atomic operations** ensure thread-safe updates to shared data structures. | Crawls **websites in parallel**, respecting **robots.txt** rules and avoiding duplicate URLs. Implements the **dining philosophers solution** to prevent deadlocks when multiple threads compete for shared resources (e.g., network connections). Uses **Breadth-First Search (BFS)** to discover new pages and **depth limits** to avoid infinite loops. **MongoDB** enables flexible storage of unstructured data (e.g., HTML, JSON). | Eclipse IDE | Docker, GitLab CI/CD |
| **Multithreading Java** | M | Real-Time Chat Server | Java SE 17, Thread Pools, Locks, Sleeping Barber, G1 GC, WebSocket, **PostgreSQL** | A **high-performance chat server** supporting thousands of concurrent users. Uses **ReentrantReadWriteLock** for thread-safe message handling, **BlockingQueue** for message buffering, and the **sleeping barber pattern** to manage thread pools efficiently. **G1 GC** minimizes garbage collection pauses, while **WebSocket** enables real-time, bidirectional communication. **PostgreSQL** stores chat history and user profiles. | Handles **real-time messaging** between users with **minimal latency**. The **sleeping barber pattern** dynamically scales the number of active threads based on demand, preventing resource waste during low traffic. **Locks** ensure thread-safe access to shared data (e.g., user lists, message queues). **WebSocket** pushes messages to clients instantly, and **PostgreSQL** persists messages for offline retrieval. Supports **group chats, private messages, and file sharing**. | IntelliJ IDEA Ultimate | AWS Elastic Beanstalk, CodePipeline |
| **Python** | B | AI-Powered Fraud Detection System | Python 3.11, Django, pytest, Pandas, **Scikit-learn**, **TensorFlow**, **Kafka**, **PostgreSQL**, Redis, Celery, FastAPI | A **Django-based backend** with **TensorFlow** for deep learning-based fraud detection. **Pandas** preprocesses transaction data (e.g., normalizing amounts, encoding categorical features), while **Scikit-learn** provides traditional ML models (e.g., Random Forest, Isolation Forest). **Celery** runs async tasks (e.g., model training, batch predictions), and **Kafka** streams real-time transaction data. **PostgreSQL** stores user and transaction records, and **Redis** caches frequent queries (e.g., user profiles). | Analyzes **user transactions in real-time** to flag suspicious activities (e.g., unusual spending patterns, geolocation mismatches). Uses **LSTM (Long Short-Term Memory) neural networks** to detect sequential anomalies (e.g., rapid successive transactions) and **ensemble methods** to combine multiple models. Integrates with **payment gateways** (e.g., Stripe, PayPal) to block fraudulent transactions before completion. **Kafka** ensures real-time data ingestion, while **Celery** schedules periodic model retraining. | PyCharm Professional | AWS EKS, GitHub Actions, ArgoCD |
| **Python** | B | Video Processing Pipeline | Python 3.11, FastAPI, NumPy, OpenCV, FFmpeg, **Kafka**, **TensorFlow**, Pinecone, Redis | A **FastAPI-based pipeline** for processing uploaded videos. **OpenCV** extracts frames and metadata (e.g., resolution, duration), while **FFmpeg** transcodes videos into multiple formats (e.g., MP4, HLS). **TensorFlow** powers deep learning models for **object detection** (e.g., YOLO, Faster R-CNN) and **face recognition**. **Kafka** streams video processing events, **Pinecone** enables vector similarity search (e.g., finding similar videos), and **Redis** caches processed results. | Processes videos to **extract metadata** (e.g., resolution, frame rate), **detect objects** (e.g., people, vehicles), and **generate thumbnails**. Uses **Kafka** to trigger downstream services, such as **analytics dashboards** (e.g., viewer engagement) or **storage systems** (e.g., S3, YouTube). **TensorFlow** models identify scenes (e.g., "beach," "city") or actions (e.g., "running," "dancing"). **Pinecone** indexes video embeddings for fast retrieval (e.g., "find all videos with cats"). | VS Code | AWS Lambda + API Gateway, AWS CodePipeline |
| **Python** | M | E-Commerce Recommendation Engine | Python 3.11, Flask, **PyTorch**, **Scikit-learn**, Pandas, Redis, SQLAlchemy, **Kafka** | A **Flask-based recommendation engine** that generates personalized product suggestions. **PyTorch** implements **collaborative filtering** (e.g., matrix factorization) and **neural collaborative filtering**, while **Scikit-learn** handles **content-based filtering** (e.g., TF-IDF, cosine similarity). **Pandas** preprocesses user behavior data (e.g., clicks, purchases), and **Kafka** ingests real-time user events (e.g., "user_viewed_product"). **Redis** caches recommendations to reduce latency. | Generates **personalized product recommendations** by analyzing **user behavior** (e.g., browsing history, past purchases) and **product attributes** (e.g., category, price). Uses **Kafka** to consume user events from the e-commerce platform, such as **clicks, add-to-cart actions, and purchases**. **Hybrid recommendations** combine collaborative and content-based approaches. **A/B testing** evaluates recommendation strategies (e.g., "users who bought X also bought Y"). | PyCharm Professional | Google Cloud Run, Cloud Build |
| **Python** | M | Automated Medical Imaging Analysis | Python 3.11, FastAPI, **TensorFlow**, OpenCV, NumPy, DICOM, **Kafka**, **MongoDB** | A **FastAPI-based system** for analyzing medical images (e.g., X-rays, MRIs, CT scans). **TensorFlow** powers **deep learning models** (e.g., U-Net for segmentation, ResNet for classification) to detect abnormalities (e.g., tumors, fractures). **OpenCV** preprocesses images (e.g., normalization, resizing), and **NumPy** handles numerical operations. **DICOM** ensures compatibility with medical imaging standards, while **Kafka** integrates with **PACS (Picture Archiving and Communication System)**. **MongoDB** stores analysis results and patient records. | Detects **abnormalities in medical images**, such as **tumors in X-rays** or **bleeding in CT scans**, and generates **detailed reports** for radiologists. Uses **Kafka** to receive images from **PACS systems** and stream results to **EHR (Electronic Health Record) systems**. **TensorFlow models** are trained on labeled datasets (e.g., chest X-rays with pneumonia annotations). **OpenCV** enhances image quality (e.g., noise reduction) before analysis. | VS Code | Azure Container Instances, Azure DevOps |
| **Machine Learning** | B | LLM-Powered Customer Support Chatbot | Python 3.11, FastAPI, **PyTorch**, **Transformers**, LangChain, Pinecone, **Kafka**, Redis, **PostgreSQL** | A **FastAPI-based chatbot** that leverages **large language models (LLMs)** for natural language understanding. **PyTorch** and **Transformers** (e.g., Hugging Face) fine-tune models on domain-specific data (e.g., customer support logs). **LangChain** orchestrates multi-step workflows (e.g., retrieving knowledge base articles, generating responses), while **Pinecone** enables **vector search** for semantic similarity. **Kafka** streams user messages and bot responses, **Redis** caches frequent queries, and **PostgreSQL** stores conversation histories. | Provides **24/7 customer support** by answering **FAQs** (e.g., "How do I reset my password?"), resolving **technical issues** (e.g., "My app crashed"), and **escalating complex queries** to human agents. Uses **Retrieval-Augmented Generation (RAG)** to ground responses in a **knowledge base** (e.g., product documentation, past tickets). **Kafka** ensures real-time message processing, while **LangChain** dynamically selects tools (e.g., search, calculation) to assist the LLM. | VS Code | AWS SageMaker, GitHub Actions |
| **Machine Learning** | B | Autonomous Drone Navigation System | Python 3.11, **TensorFlow**, **PyTorch**, OpenCV, JAX, **Kafka**, **MongoDB** | A **drone navigation system** that uses **computer vision** and **reinforcement learning** to navigate complex environments. **TensorFlow** and **PyTorch** implement **object detection** (e.g., YOLOv5) and **segmentation** (e.g., Mask R-CNN) to identify obstacles (e.g., trees, buildings). **OpenCV** processes real-time camera feeds, and **JAX** accelerates numerical computations (e.g., matrix operations). **Kafka** streams sensor data (e.g., GPS, IMU), and **MongoDB** stores mission logs and detected objects. | Enables drones to **navigate autonomously**, **avoid obstacles** (e.g., trees, power lines), and **perform tasks** (e.g., package delivery, aerial surveillance). Uses **reinforcement learning** (e.g., PPO, DQN) to optimize flight paths and **Kafka** to distribute sensor data across a fleet of drones. **OpenCV** tracks moving objects (e.g., people, vehicles) to adjust the drone’s trajectory in real-time. **MongoDB** logs mission data for post-flight analysis. | PyCharm Professional | NVIDIA Jetson (Edge), Jenkins |
| **Machine Learning** | M | Stock Market Prediction Tool | Python 3.11, FastAPI, **Scikit-learn**, **XGBoost**, **LightGBM**, Pandas, **Kafka**, **PostgreSQL** | A **FastAPI-based tool** that predicts stock prices and market trends using **machine learning models**. **Scikit-learn** provides traditional models (e.g., Linear Regression, SVM), while **XGBoost** and **LightGBM** handle gradient boosting for non-linear relationships. **Pandas** preprocesses historical data (e.g., filling missing values, normalizing features), and **Kafka** ingests real-time market data (e.g., OHLCV, news sentiment). **PostgreSQL** stores predictions and user queries. | Predicts **stock prices, volatility, and trends** using **historical data** (e.g., past prices, trading volumes) and **technical indicators** (e.g., moving averages, RSI). Uses **Kafka** to consume **real-time market data** from APIs (e.g., Alpha Vantage, Yahoo Finance). **Ensemble models** combine predictions from multiple algorithms (e.g., XGBoost + LSTM) to improve accuracy. **FastAPI** exposes endpoints for users to query predictions (e.g., "What will AAPL’s price be in 7 days?"). | Jupyter Lab | AWS Lambda, AWS CodePipeline |
| **Machine Learning** | M | Real-Time Object Detection for Retail | Python 3.11, FastAPI, **YOLOv8**, OpenCV, **Kafka**, Redis, **MongoDB** | A **FastAPI-based system** for detecting objects in retail stores using **YOLOv8** (Ultralytics), a state-of-the-art, real-time object detection model. **OpenCV** preprocesses video frames (e.g., resizing, color correction), and **Kafka** streams frames from cameras. **Redis** caches detection results to reduce latency, and **MongoDB** stores logs (e.g., detected objects, timestamps). | Detects **products, customers, and shelves** in real-time to **optimize store layouts** (e.g., placing high-demand items near entrances) and **prevent theft** (e.g., alerting staff to suspicious behavior). Uses **Kafka** to process **video feeds from multiple cameras** simultaneously. **YOLOv8** identifies objects with **high accuracy and low latency**, even in crowded scenes. **MongoDB** enables querying of historical data (e.g., "How many people visited aisle 3 yesterday?"). | VS Code | Google Kubernetes Engine (GKE), Cloud Build |
| **AWS** | B | Serverless E-Commerce Platform | AWS Lambda, API Gateway, S3, **DynamoDB**, RDS (**PostgreSQL**), SQS, SNS, CloudFormation, CloudWatch, **Kafka (MSK)**, React, TypeScript | A **fully serverless e-commerce platform** where **AWS Lambda** runs backend logic, **API Gateway** exposes RESTful endpoints, and **S3** hosts static assets (e.g., images, CSS). **DynamoDB** stores the product catalog for low-latency access, while **RDS (PostgreSQL)** manages order data with ACID compliance. **SQS/SNS** handle asynchronous tasks (e.g., order confirmations), and **Kafka (MSK)** streams real-time events (e.g., inventory updates). **CloudFormation** automates infrastructure provisioning, and **CloudWatch** monitors performance. | Supports **user authentication** (Cognito), **product listings** (DynamoDB), **cart management** (ElastiCache), **checkout** (Lambda + Stripe), and **payments** (Stripe API). **Kafka (MSK)** ensures real-time synchronization of **inventory levels** and **order statuses** across services. **React/TypeScript** powers the frontend, with **Next.js** for SSR/SSG. **S3 + CloudFront** delivers static assets globally with low latency. | AWS Cloud9 | AWS CodePipeline, AWS CodeBuild |
| **AWS** | B | **AI-Powered Log Analytics Platform** | AWS EKS, S3, RDS (**MySQL**), **Bedrock**, **SageMaker**, Lambda, Kinesis, Elasticsearch, Grafana, **Kafka (MSK)**, React | An **EKS-based log analytics platform** that uses **Amazon Bedrock** to power **LLM-based log summarization** (e.g., "Explain this error in plain English") and **SageMaker** for **anomaly detection models** (e.g., isolating unusual spikes in error rates). **Kinesis** ingests log streams, **Elasticsearch** enables fast full-text search, and **Kafka (MSK)** routes events between services. **RDS (MySQL)** stores metadata, and **Grafana** visualizes metrics. | Aggregates and analyzes **logs from microservices, servers, and applications** to detect **anomalies** (e.g., sudden traffic drops, error spikes) and provide **actionable insights** (e.g., root cause analysis). **Bedrock**’s LLMs summarize complex logs into **human-readable explanations**, while **SageMaker** models flag unusual patterns (e.g., a 500 error surge). **Kafka (MSK)** buffers and distributes logs to consumers, such as **alerting systems** or **dashboards**. **React** frontend displays logs, metrics, and alerts. | VS Code | AWS CodePipeline, ArgoCD |
| **AWS** | M | Serverless Video Transcoding Service | AWS Lambda, S3, SQS, SNS, CloudFormation, CloudWatch, FFmpeg, **Kafka (MSK)** | A **serverless video transcoding service** where **AWS Lambda** runs **FFmpeg** to convert videos into multiple formats (e.g., 720p, 1080p, HLS). **S3** stores input/output videos, **SQS** manages transcoding jobs, and **SNS** sends notifications (e.g., "transcoding complete"). **CloudFormation** deploys the infrastructure, **CloudWatch** monitors performance, and **Kafka (MSK)** streams events (e.g., job status updates). | Transcodes **uploaded videos** into **multiple formats and resolutions** for adaptive streaming (e.g., DASH, HLS). Uses **Kafka (MSK)** to notify downstream services when transcoding is complete, such as **CDNs** (e.g., CloudFront) for delivery or **analytics platforms** for viewer metrics. **Lambda** scales automatically to handle **spikes in transcoding requests**, while **SQS** ensures jobs are processed in order. | AWS Cloud9 | AWS CodePipeline |
| **AWS** | M | Real-Time IoT Data Processing | AWS IoT Core, Lambda, **DynamoDB**, Kinesis, S3, CloudWatch, **Kafka (MSK)**, Python (FastAPI) | An **IoT data processing pipeline** where **AWS IoT Core** connects and authenticates devices (e.g., sensors, actuators). **Lambda** processes incoming data (e.g., filtering, aggregation), **DynamoDB** stores device metadata (e.g., device ID, location), and **Kinesis** streams high-velocity data. **S3** archives raw data, **CloudWatch** monitors system health, and **Kafka (MSK)** distributes events to subscribers. | Processes **sensor data from IoT devices** (e.g., temperature, humidity, GPS coordinates) in **real-time**. Uses **Kafka (MSK)** to trigger **alerts** (e.g., "Temperature exceeds threshold") or **downstream analytics** (e.g., predictive maintenance). **Lambda** runs custom logic (e.g., calculating averages, detecting anomalies), while **DynamoDB** tracks device states. **Kinesis** ensures data is processed in **real-time**, even at scale (e.g., 100K+ messages/sec). | PyCharm Professional | AWS CodePipeline, Jenkins |
| **DB** | B | Financial Transaction Processing System | Java 17, Spring Boot, **Oracle DB**, Hibernate, Flyway, **Kafka**, Redis, Grafana | A **high-throughput transaction processing system** built with **Spring Boot** and **Hibernate** for ORM, connected to **Oracle DB** for ACID-compliant storage of financial data (e.g., account balances, transaction histories). **Flyway** manages database migrations, **Kafka** streams transaction events, **Redis** caches frequent queries (e.g., account balances), and **Grafana** visualizes metrics (e.g., transactions/sec, error rates). | Handles **high-volume financial transactions**, such as **deposits, withdrawals, and transfers**, with **strong consistency guarantees**. Uses **Kafka** to notify downstream services, such as **fraud detection systems** (e.g., flag unusual transactions) or **reporting tools** (e.g., generate daily transaction summaries). **Oracle DB** ensures **ACID compliance** for critical operations (e.g., transferring funds between accounts). **Redis** reduces latency for **balance lookups**. | IntelliJ IDEA Ultimate | AWS RDS for **Oracle**, GitHub Actions |
| **DB** | B | Global Social Media Analytics Platform | Python 3.11, FastAPI, **PostgreSQL**, Cassandra, Redis, **Kafka**, Elasticsearch, Grafana | A **scalable analytics platform** for social media data, using **FastAPI** for the backend. **PostgreSQL** stores relational data (e.g., user profiles, posts), while **Cassandra** handles time-series data (e.g., user activity logs). **Redis** caches trending content, **Kafka** streams real-time events (e.g., likes, shares), **Elasticsearch** powers fast search, and **Grafana** visualizes metrics (e.g., DAU, engagement rates). | Aggregates and analyzes **user interactions** (e.g., likes, shares, comments) from a **social media platform** to generate insights (e.g., "Which posts are trending?"). Uses **Kafka** to process events in **real-time**, while **Elasticsearch** enables **full-text search** of posts/comments. **Cassandra** scales to store **petabytes of activity logs**, and **PostgreSQL** manages structured data (e.g., user relationships). | PyCharm Professional | Google Cloud SQL + GKE, Cloud Build |
| **DB** | M | Inventory Management System | C# .NET 8, ASP.NET Core, **SQL Server**, Entity Framework, **Kafka**, Docker | A **.NET 8-based inventory system** using **ASP.NET Core** for the backend and **Entity Framework** for ORM, connected to **SQL Server** for data storage. **Kafka** streams inventory updates across warehouses, and **Docker** containerizes the application for easy deployment. | Tracks **inventory levels, orders, and shipments** across **multiple warehouses**. Uses **Kafka** to synchronize data in **real-time**, ensuring all locations have **up-to-date stock information**. Triggers **alerts** (e.g., "Low stock for Product X") when inventory falls below thresholds. **SQL Server** provides **transactional support** for critical operations (e.g., reserving items for an order). | Visual Studio 2022 | Azure **SQL Database**, Azure DevOps |
| **DB** | M | Real-Time Analytics Dashboard | JavaScript (Node.js), Express, **MongoDB**, Redis, **Kafka**, Grafana, React | A **Node.js-based dashboard** that uses **Express** for the backend and **MongoDB** for storing analytics data. **Redis** caches frequent queries, **Kafka** ingests real-time data from the main application, and **Grafana** visualizes metrics. **React** powers the frontend, rendering interactive charts and tables. | Displays **real-time metrics** (e.g., user activity, sales, error rates) for a **SaaS product**. Uses **Kafka** to consume events from the main application (e.g., "user_signed_up", "purchase_completed") and update the dashboard **instantly**. **MongoDB** stores **time-series data** (e.g., daily active users), while **Redis** caches **aggregated stats** (e.g., "total users this hour"). **Grafana** provides **customizable dashboards** with alerts (e.g., "Error rate > 1%"). | VS Code | Docker Swarm, GitLab CI/CD |